<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#2E2E2E"
    tools:context=".snake.Snake_Activity">

    <!-- Botón para Volver al Catálogo -->
    <ImageButton
        android:id="@+id/buttonBackToCatalog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Volver al Catálogo"
        android:padding="8dp"
        android:src="@drawable/ic_close_white"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textViewScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="24dp"
        android:text="Score: 0"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Contenedor para la Vista Personalizada del Tablero del Juego -->
    <FrameLayout
        android:id="@+id/frameLayoutBoardContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        app:layout_constraintBottom_toTopOf="@+id/linearLayoutControls"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/buttonBackToCatalog">

        <!-- Aquí se añadirá dinámicamente nuestra SnakeBoardView -->
        <!-- O puedes declararla directamente si ya la tienes: -->
        <!-- <com.proyecto.proyect_games.snake_classic.SnakeBoardView
            android:id="@+id/snakeBoardView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" /> -->
    </FrameLayout>

    <LinearLayout
        android:id="@+id/linearLayoutControls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        app:layout_constraintBottom_toTopOf="@+id/buttonPauseResume"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <Button
            android:id="@+id/buttonUp"
            style="@style/Widget.AppCompat.Button.Small"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:text="↑"
            android:textSize="24sp" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/buttonLeft"
                style="@style/Widget.AppCompat.Button.Small"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:text="←"
                android:textSize="24sp" />

            <Space
                android:layout_width="64dp"
                android:layout_height="64dp" />

            <Button
                android:id="@+id/buttonRight"
                style="@style/Widget.AppCompat.Button.Small"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:text="→"
                android:textSize="24sp" />
        </LinearLayout>

        <Button
            android:id="@+id/buttonDown"
            style="@style/Widget.AppCompat.Button.Small"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:text="↓"
            android:textSize="24sp" />

    </LinearLayout>

    <Button
        android:id="@+id/buttonPauseResume"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:text="Pause"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
